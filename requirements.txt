# ============================================================================
# SPT2025B Requirements - Updated for Python 3.11-3.12 compatibility
# ============================================================================
# Python Version Required: >=3.11, <3.13
# Note: Python 3.13+ has compatibility issues with some dependencies

# Core dependencies
streamlit>=1.28.0,<2.0.0
pandas>=2.0.0,<3.0.0
numpy>=1.24.0,<2.0.0
plotly>=5.15.0
matplotlib>=3.7.0
seaborn>=0.12.0

# Scientific computing
scikit-learn>=1.3.0
scikit-image>=0.21.0
scipy>=1.11.0
statsmodels>=0.14.0
networkx>=3.1

# Dependencies for scikit-image
lazy-loader>=0.3
imageio>=2.31.0
tifffile>=2023.7.10
PyWavelets>=1.4.1

# File I/O
h5py>=3.9.0
xmltodict>=0.13.0
lxml>=4.9.0
openpyxl>=3.1.0
Pillow>=10.0.0

# Image processing and tracking
opencv-python>=4.8.0
trackpy>=0.6.0  # Alternative to btrack for broader compatibility

# Optional advanced tracking (may have compatibility issues)
# btrack>=0.6.0  # Uncomment if needed, requires specific versions
# deeptrack>=2.0.0  # Uncomment if needed

# Advanced analysis
pywavelets>=1.4.0
hmmlearn>=0.3.0
pingouin>=0.5.3
fbm>=0.3.0

# Dependencies for pingouin
pandas-flavor>=0.6.0
tabulate>=0.9.0

# Dependencies for statsmodels
patsy>=0.5.3

# Report generation
reportlab>=4.0.4

# Testing
pytest>=7.4.0

# Utility packages
more-itertools>=10.1.0
rich>=13.5.0

# Optional: Machine Learning (comment out if not needed to save space)
# tensorflow>=2.13.0,<2.16.0  # TF 2.16+ requires Python 3.9-3.12

# Optional: Advanced segmentation (comment out if causing issues)
# segment-anything  # Requires PyTorch
# cellpose>=2.2.0  # Requires PyTorch

# SAM3 Dependencies
timm>=1.0.17
ftfy==6.1.1
regex
iopath>=0.1.10
decord
pycocotools
psutil
huggingface_hub

# Note: If you encounter import errors related to missing modules,
# ensure all optional dependencies above are uncommented
